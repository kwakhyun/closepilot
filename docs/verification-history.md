# 검증 기록

이 문서는 날짜별 검증 실행과 당시 수치를 보존한 이력이다. 현재 기준과 재현 명령은 [검증 현황](verification.md)을 우선한다.

기준일: 2026-08-31. 수치는 합성 데이터 검증 결과이며 실제 고객 성과가 아니다.

## 실행한 검사

| 경계               | 방법                           | 확인한 내용                                                               |
| ------------------ | ------------------------------ | ------------------------------------------------------------------------- |
| Domain/Application | Vitest                         | 원 단위 정수·반올림, CSV, 예외 7종, 검토 결과 해시, 마감 조건             |
| HTTP               | Vitest + 실행 중인 서버        | Origin, 요청 본문 크기, JSON 형식, 오류 응답·요청 ID, 세션                |
| Repository/DB      | 로컬 PGlite / CI PostgreSQL 17 | 행 잠금, 중복 요청, 동시 수정, 세션 분리, 트랜잭션 롤백, 수정 차단 트리거 |
| Web 전체 흐름      | `scripts/smoke-api.mjs`        | CSV 반영 → 재대사 → 검토 승인 → 마감 확정 → JSON·CSV 내보내기             |
| Kotlin/JVM         | JDK 21 + Gradle                | 원본 재계산, 검토 결과 해시, 감사 기록, 변조된 증빙 거부                  |

현재 TypeScript 테스트는 121개, Kotlin 테스트는 10개다. HTTP smoke 검사는 하나의 시나리오를 20단계로 확인하며, 별도의 단위 테스트 20개를 뜻하지 않는다. 아래 실행별 테스트 수는 해당 시점의 커밋에 포함된 수치다.

## 2026-09-01 공고 맞춤 P1·UI/UX 최종 검증

Commerce Ops 포지션의 고객 온보딩, 재사용 가능한 제품 자산, Kotlin 도메인 모델링, AI 운영 흐름을 더 직접적으로 보여주도록 기능과 화면을 확장한 뒤 다시 검증했다.

- `npm run verify`: 레이어 경계 43개 모듈, TypeScript 타입 검사, ESLint, Vitest 95개, Next.js 16 프로덕션 빌드가 모두 통과했다.
- Kotlin: JDK 21에서 9개 테스트가 통과했다. `POST /reconcile`이 프로필 요율과 주문·정산 입력으로 128행을 계산하고, `POST /verify`가 같은 유스케이스로 120개 자동 일치·8개 근거 검토·11개 감사 이벤트를 재현했다. 고정 패키지 체크섬은 `2402866290bef63d3b448df4864a5e749baafb31fe5394e0c0bbdb5adbe6c874`다.
- 온보딩 프로필: LUMIÈRE 128건·3개 채널과 MORROW FOODS 96건·2개 채널을 실제 화면에서 전환했다. MORROW FOODS 설정을 `MORROW EXPORT`로 복제해 브랜드 이름과 운영 설정이 새 세션에 고정되는 것도 확인했다.
- 프로필별 자료: MORROW 프로필의 주문 샘플은 저장된 영문 열 이름, 자사몰·쿠팡 채널과 2.9%·7.2% 가상 요율로 생성됐다. 저장 열 연결이 자동 적용되고 주문 3행을 반영해 99건이 됐다. 비활성 스마트스토어는 채널 필터와 샘플에서 제외되며, 직접 업로드하면 도메인 경계에서 거부하는 테스트를 추가했다.
- AI 검토 초안: 정산 누락 거래에서 상단 진입 버튼, 3단계 대기 안내, 읽기 전용 권한 문구를 확인했다. 실제 `gpt-5.6-luna` 응답은 `ai` 모드로 10,357ms에 생성됐고 저장된 원본 자료 ID만 인용했다.
- 세션 복구: 알 수 없거나 만료된 세션을 거부하는 저장소 테스트가 통과했다. 모든 읽기·명령·CSV 미리보기·AI 요청의 401 처리 경로는 같은 온보딩 프로필로 새 세션을 만들고, 실패한 변경 요청은 자동 재실행하지 않도록 연결했다. 실제 6시간을 기다리는 자연 만료 시나리오는 실행하지 않았다.
- 반응형·접근성: 1280×720, 390×844, 640×720(1280px 화면의 200% 확대와 같은 CSS 폭)에서 확인했다. 390px 화면의 실제 문서 너비는 스크롤바를 제외한 375px로 가로 넘침이 없었고, 640px에서도 잘린 텍스트가 없었다. 중복 ID와 이름 없는 상호작용 요소는 0개였다. 모바일 메뉴는 열릴 때 닫기 버튼으로 포커스가 이동하고 Escape 뒤 메뉴 열기 버튼으로 돌아왔다.
- 시각 비교: 기존 대시보드와 현재 대시보드를 같은 1280×720 상태로 함께 비교했다. 온보딩 메뉴 추가 후에도 카드 정렬, 표 머리글, 사이드바 폭과 주요 행동 버튼의 위치가 유지됐으며 새 화면에서도 브라우저 가로 넘침은 없었다.
- 런타임 경계: 로컬·CI에서는 Kotlin REST 서비스를 실제 실행한다. 공개 Vercel 데모의 사용자 요청은 TypeScript 인프로세스 어댑터가 처리한다는 차이를 화면과 문서에 명시했다.
- 프로덕션 배포: Vercel 배포 `dpl_E7Bxgr3VvsuwAxRXXhrrEcnz5kdh`가 `READY` 상태로 완료됐고 [closepilot-delta.vercel.app](https://closepilot-delta.vercel.app)에 연결됐다. 배포 빌드는 Next.js 16.3.3 컴파일과 타입 검사를 통과했다.
- 프로덕션 API: 20단계 전체 흐름이 7,346ms에 통과했다. 첫 배포 검사에서 본문 없는 기존 세션 요청이 400으로 바뀐 호환성 문제를 발견했고, 빈 JSON 본문을 기본 프로필로 처리하는 회귀 테스트 3개를 추가한 뒤 로컬·프로덕션에서 다시 통과시켰다.
- 프로덕션 AI: 중복 정산, 입금 시점, 수수료 차이 세 평가가 모두 `ai` 모드로 통과했고 허용된 근거 ID만 인용했다. 생성 시간은 각각 5,657ms, 6,422ms, 5,315ms였다.
- 프로덕션 화면: LUMIÈRE와 MORROW FOODS 전환, MORROW의 2개 채널 필터, 프로필별 CSV 3행과 저장 열 연결 적용, AI 초안 상단 진입점을 확인했다. 390×844 화면의 문서 너비는 375px로 가로 넘침이 없었고 브라우저 오류·경고 로그는 0건이었다. Vercel 최근 오류 로그도 0건이었다.

로컬 API 실행 기록은 [api-smoke-local.json](evidence/api-smoke-local.json)에 있다. 고정된 입력으로 만든 기준 마감 증빙은 [closed-package.json](../fixtures/closed-package.json), 기준 합계는 [baseline.json](../fixtures/baseline.json)을 참고한다.

## GitHub Actions 실행 결과

2026-08-31, 커밋 `e9dab668427f5475df6b8107067e9c9c16ad3a0d`의 [실행 #33348712211](https://github.com/kwakhyun/closepilot/actions/runs/33348712211)에서 `web`, `kotlin` 작업 모두 `success`로 완료됐다. 이 구간은 최초 배포 커밋의 기록이다.

- **Web / Ubuntu / Node.js 24 / PostgreSQL 17**: 포맷, 레이어 경계, 타입, lint, TypeScript 테스트 77개, 프로덕션 빌드, 고정 데이터 재생성 일치, 실행 중인 API의 20개 검증 단계를 통과했다. DB 테스트와 HTTP 흐름 모두 실제 PostgreSQL 서비스에 연결했다.
- **Kotlin / Ubuntu / JDK 21**: 테스트와 독립 패키지 재계산을 통과했다. 출력은 `128 rows, 120 matches, 8 evidence-backed reviews, 11 audit events`였다.

실행 페이지의 `web-verification` 아티팩트에는 HTTP 결과 JSON과 서버 로그, `kotlin-verification`에는 테스트 HTML 보고서가 있다. 아티팩트는 GitHub 보존 기간이 지나면 만료될 수 있으며, 아래 명령과 저장소의 고정 입력으로 재실행할 수 있다. CI 통과는 해당 입력·환경의 정확성 검증이며 운영 부하나 SLA 검증이 아니다.

## 고정 입력 재현

```bash
npm ci
npm run verify
npm run fixtures
cd verifier
bash gradlew test run --args='../fixtures/closed-package.json'
```

기준 마감 증빙에는 대사 결과 128행, 자동 일치 120건, 검토 승인 8건, 감사 이벤트 11건이 포함된다. 체크섬은 다음과 같다.

```text
16713c14726c4498278c01fef4f1623c330b24dedc4756dfeb98d7d60fd5b3ab
```

## 실제 HTTP 흐름

세션 2개를 만들고 서로 다른 쿠키로 구분한다. 같은 요청 키로 재시도하는 경우, 같은 키에 다른 본문을 보내는 경우, 동시에 버전을 수정하는 경우를 검사한다. 정상·오류 CSV, 재대사 조건, 미검토 거래가 있을 때의 마감 차단, 마감 후 수정 차단과 내보내기도 확인한다. 샘플 CSV를 반영한 최종 결과는 전체 131건, 자동 일치 123건, 검토 승인 8건이다. 보고서에는 토큰을 포함하지 않는다.

## 브라우저와 공개 배포

공개 URL은 [closepilot-delta.vercel.app](https://closepilot-delta.vercel.app)이다. Vercel에 전용 Neon PostgreSQL을 연결했다. 공개 API 검증 20단계를 통과했으며 [실행 보고서](evidence/api-smoke-production.json)에 실행 시각과 결과를 기록했다. 최초 실행의 7,977ms는 검증 스크립트 전체의 소요 시간으로, API 응답 시간이나 처리량을 측정한 벤치마크가 아니다.

최초 공개 API 검사에서 내려받은 증빙은 Kotlin CLI에서도 **대사 결과 131행, 자동 일치 123건, 검토 승인 8건, 감사 이벤트 17건**으로 재계산을 통과했다. 체크섬은 `d8ba21a305cbd18f0bd4f3a461da8f68d248d03bfe527ff4236f6f35340491f0`이다. 이 임시 파일에는 가상 거래만 포함되어 있으며 공개 보고서와 별도로 로컬 `.data`에 보관했다.

브라우저에서 주문·정산 CSV 샘플의 열 연결, 미리보기, 반영을 확인했다. 재대사 전 마감 차단, 예외 거래 8건의 검토 승인, 마감 확정과 다운로드 링크도 확인했다. 실행한 UI 흐름의 최종 상태는 대사 결과 131행, 자동 일치 123건, 검토 승인 8건이며 원본 순차액 37,353원을 유지했다. 검토 가이드의 규칙 기반 표기, 차트의 채널별 전환, 표의 다음·이전 페이지도 확인했다.

좁은 화면에서는 표의 스크린 리더용 숨김 요소가 스크롤 영역 밖으로 나가 페이지 너비를 늘리는 문제를 발견했다. 표의 스크롤 영역을 위치 기준으로 지정해 수정했다. 너비 390px 화면에서 문서 너비가 375px(스크롤바 제외)로 유지되어 가로로 넘치지 않는 것을 확인했다. 제품 가이드도 같은 너비에서 검사했다. 화면 캡처는 `docs/evidence`에 저장한다.

## 화면 증빙

캡처: [대시보드](evidence/dashboard.jpg), [원본 자료와 거래 검토](evidence/review.jpg), [모바일](evidence/mobile.jpg). 최초 공개 데모의 데스크톱 화면에서 브라우저 콘솔 로그를 조회했을 때 반환된 항목은 0개였다. 이 결과는 확인한 시점의 기록이며 이후 모든 상황에서 오류가 발생하지 않는다는 보장은 아니다.

## 문구 교정 후 재검증

2026-08-31, 화면·오류 안내·제품 가이드와 내부 문서를 [용어 기준](copy-guide.md)에 맞춰 교정했다. 계산 규칙, 저장 형식, API 필드명은 변경하지 않았다.

- `npm run verify`: 레이어 경계, 타입, lint, TypeScript 테스트 77개와 배포용 빌드 통과.
- `npm run fixtures` 후 `git diff --exit-code -- fixtures migrations`: 변경 없음. 기준 마감 파일의 체크섬도 유지됐다.
- Kotlin 테스트 6개와 기준 마감 파일의 독립 재계산 통과.
- 브라우저: CSV 열 중복 연결 오류와 재검증, 주문·정산 샘플 반영, 재대사 전 마감 차단, 예외 8건의 검토 승인, 마감 확정, 감사 기록 재검증을 확인했다. 완료 후에도 대사 결과 131행, 자동 일치 123건, 검토 승인 8건, 원본 순차액 37,353원을 유지했다.
- 교정 전에 만든 로컬 마감 세션과 공개 데모의 검토 승인도 유효하게 표시됐다. v1 증빙에 포함된 설명과 감사 원문은 보존하고, 화면에 표시할 때만 교정한 문구를 적용했다.
- 1440px 데스크톱과 390px 모바일에서 문구와 줄바꿈을 확인했다. 모바일 자료 가져오기 대화상자와 제품 가이드 모두 페이지 너비가 뷰포트를 넘지 않았다. 없는 페이지의 안내와 마감 후 읽기 전용 상태도 확인했다.

교정본의 공개 API 검증 20단계도 통과했다. 실행 시각과 결과는 [후속 검증 보고서](evidence/api-smoke-copy-review.json)에 기록했다. 전체 스크립트 실행 시간은 8,048ms이며 성능 벤치마크가 아니다. 이 실행에서 내려받은 증빙은 Kotlin에서도 **131행, 일치 123건, 검토 승인 8건, 감사 이벤트 17건**으로 재계산을 통과했다. 체크섬은 `444735a3ace1331a77d466c7b5ec92f00792baab229f0f282b816c7fdc56829a`이다. 최초 공개 검증 보고서는 별도로 보존했다.

README의 대시보드·거래 검토·모바일 캡처도 교정본으로 갱신했다. 이번 문구 검토에는 외부 LLM API를 사용하지 않았고, 새 API 키를 발급하지 않았다.

## P0 기능·UI/UX 개선 검증

2026-09-01, 예외 거래를 찾고 연속으로 검토하는 핵심 동선을 우선 개선한 뒤 로컬 빌드와 실제 브라우저에서 다시 검증했다.

- `npm run verify`: 레이어 경계 35개 모듈, 타입 검사, lint, TypeScript 테스트 80개, Next.js 프로덕션 빌드가 모두 통과했다.
- 대시보드: 안내 카드의 기본 동작이 `확인할 거래 보기`로 연결되고, 확인 필요 거래 목록이 차트보다 먼저 표시되는 것을 확인했다. 사이드바의 `거래 대사 8`도 확인 필요 탭으로 바로 이동한다.
- 대사 목록: 전체 거래 탭에서도 미검토 예외 8건이 먼저 정렬되며, 차액이 0원인 입금 확인 필요 거래 2건도 자동 일치 거래보다 앞에 남는다. 탭은 `tablist`·`tab`·`tabpanel` 관계를 갖고, 왼쪽·오른쪽 화살표와 Home·End 키로 선택과 포커스가 함께 이동한다.
- 연속 검토: `LM-2608045`에서 검토 예시와 원본 확인을 입력한 뒤 `기록하고 다음 거래 보기`를 실행했다. 미검토 건수가 8건에서 7건으로 줄고 대사 처리율이 94%에서 95%로 바뀌었으며, 다음 우선순위 거래 `LM-2608063`이 같은 서랍에서 열렸다. 기록 전에는 사유·증빙·원본 확인·최신 대사의 충족 여부가 각각 표시된다.
- 자료 가져오기: 1280×720 화면에서 샘플 주문 3행을 검증한 상태로 `자료 반영` 버튼의 상단 639px, 하단 675px, 화면 높이 720px을 확인했다. 긴 열 연결과 미리보기 내용은 내부에서 스크롤되고 실행 버튼은 화면에 남는다.
- 모바일 메뉴: 390×844 화면에서 문서 너비 375px과 화면의 콘텐츠 너비 375px이 같아 가로 넘침이 없었다. 메뉴가 열리면 본문에 `inert`가 적용되고 스크롤이 잠긴다. 첫 항목에서 Shift+Tab을 누르면 마지막 항목으로, 마지막 항목에서 Tab을 누르면 첫 항목으로 이동하며, Escape로 닫은 뒤 `메뉴 열기` 버튼에 포커스가 돌아왔다.
- 제품 가이드: 모바일 본문과 보조 문구의 글자 크기를 높이고 저대비 색을 교체했다. `#5b6d52`는 흰색에서 5.59:1, `#f7f8f3`에서 5.24:1이며, `#526447`은 각각 6.42:1과 6.01:1이다.
- 화면 증빙: 동일한 데모 데이터로 데스크톱 대시보드, 거래 검토 서랍, 390px 모바일 화면을 다시 캡처해 `docs/evidence`에 반영했다.

## 검증하지 않은 범위

실제 PG·은행 연동, 고객 자료, 대량·장시간 트래픽, 여러 승인자의 작업, 보안 침투 테스트, 백업 복구, Docker 실행, 회계·개인정보 관련 법적 요건 충족 여부는 검증하지 않았다. 로컬 PGlite와 CI PostgreSQL의 테스트 통과를 클라우드 운영 성능의 근거로 사용하지 않는다.

## 2026-09-01 전체 개선 검증

P1·P2 개선과 AI 검토 초안, Kotlin HTTP 경계, 관측성 보강을 마친 뒤 다음 항목을 다시 확인했다.

- `npm run verify`: 아키텍처 경계 41개 모듈, TypeScript 타입 검사, ESLint, Vitest 85개, Next.js 16 프로덕션 빌드가 모두 통과했다.
- `npm run fixtures`: 합성 자료 128행과 예외 거래 8건을 재생성했으며 체크섬 `16713c14726c4498278c01fef4f1623c330b24dedc4756dfeb98d7d60fd5b3ab`이 유지됐다. `fixtures`와 `migrations`에는 예상하지 않은 변경이 없었다.
- `npm run smoke -- http://localhost:3000`: 세션 격리, 원자적 가져오기, 멱등성, 동시성, 대사, 검토, 마감, 내보내기, 감사 해시를 다루는 20단계가 모두 통과했다.
- `npm run benchmark -- http://localhost:3000`: 30회 제한 요청, 동시성 5에서 오류율 0%, p50 55.9ms, p95 78.5ms였다. 이 수치는 로컬 작업 공간 읽기 경로의 회귀 확인용이며 용량이나 SLA 지표가 아니다.
- Kotlin 검증기: JDK 21에서 7개 테스트가 통과했고, 동일한 합성 마감 패키지에서 128행, 자동 일치 120건, 근거가 있는 검토 8건, 감사 이벤트 11건을 재현했다. CLI와 `POST /verify`가 같은 검증 함수를 사용한다.

승인된 OpenAI 프로젝트 키로 `gpt-5.6-luna` 기반 검토 초안을 실제 평가했다. 중복 정산, 입금 시점 차이, 수수료 차이 세 사례가 모두 `ai` 모드로 생성됐고, 허용된 원본 자료·정산 ID만 인용했다. 생성 시간은 각각 5,203ms, 4,453ms, 4,248ms였다. 에이전트는 읽기 전용 근거 조회 도구만 사용할 수 있고, 초안을 적용해도 확인 체크박스와 승인·마감 상태는 바뀌지 않는다. API 키, 요청 본문, 프롬프트는 로그에 기록하지 않는다.

1280×720 브라우저에서 URL 기반 화면 이동, 뒤로 가기 복원, 제목 포커스와 상단 스크롤, CSV 가져오기 단계 표시와 고정 푸터, 마감 모달의 독립 스크롤과 고정 행동 영역, AI 초안 생성·적용 범위를 확인했다. 브라우저 콘솔에는 애플리케이션 오류가 없었다. 개선 전후 화면을 같은 크기로 나란히 비교해 잘린 요소, 불필요한 포커스 테두리, 간격과 정렬을 재검토했고 발견한 제목 테두리를 제거했다.

## 2026-09-01 프로덕션 재검증

커밋 `0767765` 기준 결과를 Vercel 프로덕션에 배포했다. 배포 ID는 `dpl_364G3SRozWe8PG8VrKTuxCrzAw7j`, 상태는 `READY`이며, 공개 별칭은 [closepilot-delta.vercel.app](https://closepilot-delta.vercel.app)이다. `OPENAI_API_KEY`는 Vercel의 Production, Preview, Development 환경에 Secret 유형으로 등록했으며 값은 로그와 저장소에 노출하지 않았다.

- 원격 smoke 검증 20단계가 모두 통과했다. 실행 시간은 6,958ms이며 합성 세션의 전체 업무 흐름을 확인한 값이다.
- 중복 정산, 입금 시점 차이, 수수료 차이 AI 평가 3건이 모두 `ai` 모드로 통과했다. 생성 시간은 각각 9,387ms, 4,273ms, 4,998ms였고 허용된 원본 자료·정산 ID만 인용했다.
- 브라우저에서 별도의 정산 누락 거래가 15초 제한을 넘겼을 때 `TimeoutError`를 기록하고 HTTP 200의 규칙 기반 초안으로 전환되는 것을 확인했다. 원본 금액, 검토 상태, 마감 상태는 바뀌지 않았고 사용자는 전환 사실을 화면에서 확인할 수 있었다.
- 배포 후 Vercel 오류 수준 로그와 5xx 요청은 없었다. AI 로그에는 요청 ID, 모델, 토큰 수, 지연 시간, 전환 유형만 남고 키·프롬프트·요청 본문은 남지 않았다.
- 프로덕션 브라우저 콘솔 오류는 없었다. 홈, `robots.txt`, `sitemap.xml`, Open Graph 이미지가 모두 HTTP 200으로 응답했고 canonical 및 Open Graph 메타데이터가 포함됐다.
- [GitHub Actions #33465250321](https://github.com/kwakhyun/closepilot/actions/runs/33465250321)에서 Web과 Kotlin 작업이 모두 성공했다. `actions/checkout@v7`, `actions/setup-node@v7`, `actions/setup-java@v6`, `actions/upload-artifact@v7`로 갱신해 이전 런타임 지원 종료 경고도 제거했다.

## 2026-09-04 P0·P1 포트폴리오 보강 검증

채용 검토자가 짧은 시간에 완성 상태를 확인할 수 있도록 완료형 합성 예시와 90초 경로를 추가하고, 계산 계약과 회귀 검증 범위를 넓혔다. 실제 고객 인터뷰나 도입 성과는 새로 주장하지 않았으며, 발견 기록은 빈 템플릿만 추가했다.

- 규칙 버전: CSV 내보내기와 fixture 생성기가 공통 `RULE_VERSION`을 사용하도록 고쳤다. CSV 행에 `krw-net-v1.1.0`이 기록되는 회귀 테스트를 추가했고 기준 fixture도 같은 버전으로 재생성했다.
- 완료형 예시: 일반 명령으로 예외 8건을 검토하고 마감한 작업공간을 만들며, 화면에서 합성 예시임을 표시한다. 자료 반영과 재대사는 처음부터 비활성화된다.
- UI 구조: 1,021줄 대시보드를 575줄 조합 컴포넌트와 세션 훅, 내비게이션, 요약·거래 섹션으로 분리했다. 도메인·application 경계는 유지했다.
- AI 경계: 허용되지 않은 근거 ID뿐 아니라 원화 기호, KRW, 만·억·조 단위의 금액과 여러 날짜 표기, 승인·마감·송금 완료 표현의 변형을 거부한다. 사용자가 정한 파일명은 모델 근거에서 제외하고, 적용되는 증빙 참조는 검증한 인용 ID만으로 서버가 다시 구성한다. 합성 평가 사례는 5건으로 늘렸다.
- `npm run verify`: 아키텍처 경계 48개 모듈, 타입 검사, ESLint, Vitest 12개 파일·121개 테스트, Next.js 프로덕션 빌드를 통과했다.
- `npm run test:coverage`: 명시한 domain/application/http/repository 범위에서 statements 91.56%, branches 84.86%, functions 92.96%, lines 91.95%였다.
- `npm run test:e2e`: 7개가 통과했다. 샘플 주문·정산 반영부터 재대사, 예외 8건 검토, 마감 확정, JSON 다운로드까지 브라우저로 완주하고 완료형 예시의 읽기 전용 상태도 확인했다.
- Kotlin/JDK 21: 10개 테스트와 고정 패키지 재계산을 통과했다. TypeScript가 생성한 대표 대사 행 계약을 Kotlin `/reconcile` HTTP 응답과 비교했다. 기준 패키지는 128행, 자동 일치 120건, 검토 8건, 감사 이벤트 11건이며 체크섬은 `2402866290bef63d3b448df4864a5e749baafb31fe5394e0c0bbdb5adbe6c874`다.
- 로컬 프로덕션 HTTP smoke 20단계가 모두 통과했고 전체 실행은 682ms였다. 이 값은 합성 흐름의 회귀 확인 시간이며 API 성능이나 SLA 지표가 아니다.
- AI 평가 스크립트는 서버와 같은 근거 검증기를 사용해 규칙 기반 fallback 5건을 통과했다. `--require-ai` 실행은 5건 모두 `rules` 모드인 로컬 키 미설정 환경을 성공으로 오인하지 않고 종료 코드 1을 반환했다. 실제 모델 응답 평가는 공개 배포 후 별도로 실행한다.
- `npm audit --omit=dev --audit-level=high`: 프로덕션 의존성의 알려진 취약점 0건으로 완료됐다.
- 시각 검수: 제품 가이드 전체를 1440×900과 390×844로 캡처하고, 완료형 첫 화면을 1440×900에서 확인했다. 90초·완료형·설계 링크 버튼의 줄바꿈, 네 단계 흐름, 구현/미구현 카드, 푸터와 완료형 알림·다운로드 링크에서 겹침이나 잘림을 발견하지 않았다.

커밋 `993bcc7`을 Vercel 프로덕션 배포 `dpl_6xrht6CHG6dX6uhKicwrBzVYfr8g`에 반영했고 상태는 `READY`다. 공개 별칭은 [closepilot-delta.vercel.app](https://closepilot-delta.vercel.app)이며 Node.js 24 런타임과 관리형 PostgreSQL을 사용한다.

- [GitHub Actions #33833005110](https://github.com/kwakhyun/closepilot/actions/runs/33833005110): PostgreSQL 17을 사용하는 Web 작업과 JDK 21 Kotlin 작업이 모두 성공했다. Web 작업은 포맷, 전체 검증, 커버리지, Playwright 7개, fixture 재생성 일치, 실행 중인 API의 20단계를 포함한다.
- 공개 HTTP smoke: 20단계가 모두 통과했고 전체 실행은 8,129ms였다. [실행 보고서](evidence/api-smoke-production-20260904.json)는 합성 세션의 검사 이름과 상태만 기록하며 쿠키, 토큰, DB 연결 문자열을 포함하지 않는다.
- 공개 AI 평가: `--require-ai`를 사용한 중복 정산, 입금 시점, 수수료 차이, 정산 누락, 환불액 차이 5건이 모두 `ai` 모드로 통과했다. 생성 시간은 각각 5,897ms, 5,067ms, 4,788ms, 5,259ms, 4,414ms였고 근거 검증 오류는 없었다.
- 공개 화면·상태: `/api/health`는 PostgreSQL 합성 샌드박스를 반환했다. 제품 가이드에서 90초 경로와 완료된 합성 예시 링크를 확인했고, 완료형 세션은 `completed-showcase`, `closed`, 미검토 0건으로 응답했다.
- 검증 직후 Vercel 오류 수준 로그는 0건이었다. 이는 확인한 시간 구간의 결과이며 장기 무오류나 운영 SLA를 뜻하지 않는다.
