# Optional locally: absent DATABASE_URL uses embedded PostgreSQL in .data/.
# Required on Vercel: a dedicated PostgreSQL database, never a customer's database.
DATABASE_URL=
# The app always runs as a synthetic-data sandbox; there is no live-money mode.
# Optional origin override for reverse proxies. Do not use wildcard origins.
APP_ORIGIN=http://localhost:3000
